from tkinter import*

~root.mainloop()



★ basic frame

root=Tk()
root.title("GUI name")
root.geometry("width x height + x pos + y pos")
root.resizable(False,False) # fixed width,height



★ button

btn1=Button(root,text="button")
btn1.pack() # 버튼 가시화

btn2=Button(root,padx=?,pady=??,text="버튼") #버튼 여백을 ?*??로 고정
btn2.pack()

btn3=Button(root,width=?,height=??,text="ボタン") # ボタンサイズを?*??に固定
btn3.pack()

btn4=Button(root,fg="text color",bg="bg colort",text="color button")
btn4.pack()

photo=photoImage(file="?/?.png")
btn5=Button(root,image=photo)
btn5.pack()

def btncmd():
     print("clicked")
btn6=Button(root,text="click me",command=btncmd)
btn6.pakc()

def mns(a,b):
     print(a-b)
btnm=Button(root,text="m",command=lambda:mns(6,3))
btnm.pack()




★ label

label1=Label(root,text="Hi")
label1.pack()

photo = PhotoImage(file="?/?.png")
label2=Label(root,image=photo)
label2.pack()

def change():
     label1.config(text="おはよー")
     global photo2
     photo2=PhotoImage(file="?/!.png")
     label2.config(image=photo2)

btn Button(root,text="click me",command=change)
btn.pack()



★ text,entry

txt=Text(root,width=30,height=5)
txt.pack()

text.insert(END,"text in")


e=Entry(root,width=30)
e.pack()

e.insert(0,"Entry는 한줄만 입력가능") #현재 값이 비어있으므로 END를 써도 동일

def btncmd():
     #내용 출력
     print(txt.get("?.!",END)) #?: ?行  !: !번째 column
     print(e.get())
     #내용 삭제
     txt.delete("?.!",END)
     e.delete(0,END)

btn=Button(root,text="click me",command=btncmd)
btn.pack()



★ listbox

listbox=Listbox(root,selectmode="extended",height=0 or n)
#selectmode="single" #height=0 ->listbox내의 모든값이 보이게
listbox.insert(0,"사과")
listbox.insert(1,"apple")
listbox.insert(END,"アップル") #END 最後に追加
listbox.pack()

def btncmd():
     #삭제
     listbox.delete(END) #最後から削除
     listbox.delete(0) #最初から削除

     #갯수 확인
     print(listbox.size())

     #항목확인 (시작 idx, 끝 idx)
     print("1번부터 3번 항목 : ",listbox.get(0,2))

     #선택된 항목확인 (위치로 변환 (ex) (1,2,3))
     print("selected : ",listbox.curselection())

btn=Button(root,text"click me",command=btncmd)
btn.pack()



★ checkbox

chkvar=IntVar()  #chkvar에 int형으로 값을 저장
chkbox=Checkbutton(root,text="check it",variable=chkvar)
chkbox.select() #자동 선택처리
chkbox.deselect() #선택 해제 처리
chkbox.pack()

def btncmd():
     print(chkvar.get()) # 0 : 체크해제  1 : 체크



★ radiobutton

Label(root,text="order main").pack

burger_var=IntVar()
btn_burger1=Radiobutton(root,text="ハンバーガー",\
     value=1,variable=burger_var)
btn_burger1.select()
btn_burger2=Radiobutton(root,text="チーズバーガー",\
     value=2,variable=burger_var)
btn_burger3=Radiobutton(root,text="チキンバーガー",\
     value=3,variable=burger_var)
btn_burger1.pack()
btn_burger2.pack()
btn_burger3.pack()

Label(root,text="order drink")

drink_var=StringVar()
btn_drink1=Radiobutton(root,text="コーラ",\
     value="コーラ",variable=drink_var)
btn_drink1.select()
btn_drink2=Radiobutton(root,text="サイダー",\
     value="サイダー",variable=drink_var)
btn_drink1.pack()
btn_drink2.pack()

def btncmd():
     print(burger_var.get())  #선택된 라디오 항목의 값(value) 출력
     print(drink_var.get())

Button(root,text="order",comman=btncmd).pack()



★ combobox

import tkinter.ttk as ttk

val=[str(i)+"日" for i in range(1,32)]
combobox=ttk.Combobox(root,height=n,value=val)
combobox.pack()
combobox.set("日付") #최초 제목 설정

readonly_combobox=ttk.Combobox(root,height=n,\
     value=val,state="readonly")
readonly_combobox.pack()
readonly_combobox.current(0) #0번째 인덱스 값 선택

def btncmd():
     print(combobox.get())
     print(readonly_combobox.get())

Button(root,command=btncmd).pack()



★ progressbar

progressbar=ttk.Progressbar(root,maximum=100\
     mode="indeterminate") #往復

progressbar=ttk.Progressbar(root,maximum=100\
     mode="determinate") #片道
progressbar.pack()

def btncmd():
     progressbar.stop() #작동 중지
Button(root,text="stop",command=btncmd).pack()


p_var2=DoubleVar()  #정수값만이 아닌 실수도 정의
progressbar2=ttk.(root,maximum=100,length=150\
     variable=p_var2)
progressbar2.pack()

def btncmd2():
     for i in range(1,101):
          sleep(0.01)
          p_var2.set(i)
          progressbar2.update() #업데이트
          print(p_var.get())
Button2(root,text="start",command=btncmd2).pack()



★ menu

menu= Menu(root) #ここでmenuは特に意味ない

def creat_new_file:
     print("新しいファイルを作りました.")
menu_file=Menu(menu,tearoff=0)
memu_file.add_command(label="New File",\
     command=creat_new_file)
menu_file.add_command(label="New Window")
menu.add_separator()  #줄 구분
menu_file.add_command(label="Open File...")
menu_file.add_separator()
menu_file.add_command(label="Save All",\
     state="disable") #비활성화
menu_file.add_separator()
menu_file.add_command(label="Exit",\
     command=root.quit) #root 종료
menu.add_cascade(label="File",menu=menu_file) #메뉴 가시화

menu.add_cascade(label="Edit")

menu_lang=Menu(root,tearoff=0)
menu_lang.add_radiobutton(label="KOR")
menu_lang.add_radiobutton(label="JAP")
menu_lang.cascade(label="Language,menu=menu_lang")

menu_view=Menu(root,tearoff=0)
menu_view.add_checkbox(label="Show Minimap")
menu_view.cascade(label="View",menu=menu_view)

root.config(menu=menu) #必ずいる.앞쪽menu는코딩용어



★ messagebox

from tkinter.messagebox as msgbox

def info():
     msgbox.showinfo("title","message")
Button=(root,text="info",command=info).pack()

def error():
     msgbox.showerror("tile","message")
Button=(root,text="error",command=error).pack()

def okcancel():
     msgbox.askokcancel("title","message")
Button=(root,text="okcancel",command=okcancel).pack()

def retrycancel():
     response=msgbox.askretrycancel("title",\
          "message")
     if response==1:
          print("Retry")
     elif response==2:
          print("No")
Button=(root,text="retrycancel",command=retry).pack()

def yesno():
     msgbox.askyesno("title","message")
Button=(root,text="yes no",command=yseno).pack()

def yesnocancel():
     response=msgbox.askyesnocancel(title="?",\
          message="?")
     print(response) #True False None
     if response==1: #yes
          print("Yes")
     elif response==2: # No
          print("No")
     else:
          print("Cancel")
Button=(root,text="yes no cancel",command=yesnocanel).pack()



★ frame

Label(root,text="menu").pack(side="top") #side=side
Button(root,text="order").pack(side=bottom)

frame_burger=Frame(root,relief="solid",bd=1) #relief 테두리 모양 bd 외각선
frame_burger.pack(side="left",fill="both",\
     expand=True) #fill="both"->맨밑까지 채움(?),expand 빈공간없이 채움
Button(frame_burger.text="ハンバーガー").pack()
Button(frame_burger.text="チーズバーガー").pack()
Button(frame_burger.text="チキンバーガー").pack()

frame_drink=LabelFrame(root,text="ドリンク")#FrameLabel タイトルがあるフレーム
frame_drink.pack(side="right",fill="both",expand=True)
Button(frame_drink,text="コーラ").pack()
Button(frame_drink,text="サイダー").pack()



★ scrollbar

frame=Frame(root) #프레임에 넣어두는것이 안정적
frame.pack()

scrollbar=Scrollbar(frame)
scrollbar.pack(side="right",fill="y") #y축으로 채움

listbox=Listbox(frame,selectmode="extended",\
     height=10,yscrollcommand=scrollbar.set) #set이 없으면 스크롤을 내려도 다시 올라옴

for i in range(1,32):
     listbox.insert(End,str(i),"日")
listbox.pack(side="left")

scrollbar.config(command=listbox.yview) #리스트박스-스크롤바 동기화



★ grid

(0.0) (0.1) (0.2)  (row.column)
(1.0) (1.1) (1.2)
(2.0) (2.1) (2.2)

btn_alt=Button(root,text="Alt")
btn_alt.grid(row=3,column=2)
btn_space=Button(root,text="space")
btn_space.grid(row=3,column=3,\
     columnspan=5) #column 5개를 합침
btn_row=Button(root,text="row")
btn_row.grid(row=2,column=3,rowspan=2) #row 2개합침



★ advanced grid

btn_alt=Bottun(root,text="alt",padx=5,pady=5) #버튼 여백사이즈
btn_alt.grid(row=3,column=2,padx=3,pady=3\ #버튼간 여백
     sticky=N+E+W+S) #sticky 東西南北



★ filedialog

askopenfilenameとは単数,複数の違い

def add_file():
     files=filedialog.askopenfilenames(title="!!",\
          filetypes=(("PNG 파일,"*.png"),("모든파일","*.*")),\
          initialdir=r"最初経路")   #r로 탈출문자 사용
     for file in files:
          list_??.insert(END,file)

def_file():
     for index in reversed(list_??.curselection()):
          list)??.delete(index)

def browse_dest_path():
     folder_selected=filedialog.askdirectory()
     if folder_selected==''  #何も選択されてない
          return



★ Image

from PIL import Image      #pip install Pillow

def merge_image():
     images=[Image.open(x) for x in list_??.get(0,END)]
     widths,heights=zip(*(x.size for x in images))
     #size -> size[0] : width, size[1] : height
     #withs=[x.size[0] for x in images]
     #heights=[x.size[1] for x in images]

     max_width,total_height=max(widths),sum(heights)
     result_img=Image.new("RGB",\(max_width[横],total_height[縦]),\
          (R,G,B)) # (R,G,B)-背景色 ex) (255,255,255)白

     y_offset=0  #y位置を設定
     for idx,img in enumerate(image):
          result_img.paste(img,(0,y_offset))  #??.paste로 사진붙이기
          y_offset+=img.size[1]

          progress=(idx+1)/len(images)*100 #percent
          p_var.set(progress)
          progress_bar.update()



★ 저장경로

from os import*

result_img.save(path.join("저장경로","ファイル名.形式"))



★ font

font=Font(family="フォント名",size=16(普段),

l1=label(root,text="aaa",font=font)
entry=Entry(root,width=30)
l1.pack()
entry.pack()

entry.configure(font=("フォント名",フォントサイズ)) #写真参考